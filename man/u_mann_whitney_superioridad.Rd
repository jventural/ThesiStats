% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/u_mann_whitney_superioridad.R
\name{u_mann_whitney_superioridad}
\alias{u_mann_whitney_superioridad}
\title{Mann-Whitney U Test with the Probability of Superiority}
\usage{
u_mann_whitney_superioridad(data, formula, alternative = "two.sided")
}
\arguments{
\item{data}{A data frame.}

\item{formula}{A formula \code{outcome ~ group}; the group must have exactly two
levels.}

\item{alternative}{Alternative hypothesis passed to \code{\link[stats:wilcox.test]{stats::wilcox.test()}}.}
}
\value{
A tibble with the test results (\code{statistic} is U for the first
group, \code{p.value}, \code{method}, \code{alternative}), \code{PSest} and \code{Interpretación}.
}
\description{
Runs a Mann-Whitney U test (Wilcoxon rank-sum) between two groups and adds
the probability of superiority, \eqn{PS = U / (n_1 n_2)}: the probability
that a random case of the first group scores higher than a random case of
the second.
}
\details{
PS = .50 means no effect. The size is judged on the distance from
.50 in either direction, using max(PS, 1 - PS): at least .71 is
"Grande", at least .64 "Mediano", at least .56 "Pequeño" and below that
"No efecto".
}
\examples{
set.seed(1)
df <- data.frame(grupo = rep(c("A", "B"), each = 30),
                 puntaje = c(rnorm(30, 10), rnorm(30, 11)))
u_mann_whitney_superioridad(df, puntaje ~ grupo)
}
