% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epsilon_cuadrado_kruskal.R
\name{epsilon_cuadrado_kruskal}
\alias{epsilon_cuadrado_kruskal}
\title{Kruskal-Wallis Test with Epsilon Squared}
\usage{
epsilon_cuadrado_kruskal(data, formula)
}
\arguments{
\item{data}{A data frame.}

\item{formula}{A formula \code{outcome ~ group}.}
}
\value{
A tibble with the test results (\code{statistic}, \code{p.value},
\code{parameter}, \code{method}), \code{Epsilon} and \code{Interpretación}.
}
\description{
Runs a Kruskal-Wallis test and adds its effect size, epsilon squared
\eqn{\epsilon^2 = H / (n - 1)}, with a verbal interpretation.
}
\details{
\code{n} counts the cases with non-missing outcome and group, the same
cases used by the test. The interpretation is "Grande" for
\eqn{\epsilon^2} >= .50, "Mediano" >= .30, "Pequeño" >= .10 and
"No significativo" otherwise.
}
\examples{
df <- data.frame(
  grupo = rep(c("A", "B", "C"), each = 5),
  puntaje = c(3, 4, 5, 4, 3, 7, 8, 6, 7, 8, 10, 12, 11, 9, 10)
)
epsilon_cuadrado_kruskal(df, puntaje ~ grupo)
}
