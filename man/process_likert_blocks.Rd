% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remplace_alternative_response.R
\name{process_likert_blocks}
\alias{process_likert_blocks}
\title{Detect and Score Several Blocks of Likert Items}
\usage{
process_likert_blocks(df, specs)
}
\arguments{
\item{df}{A data frame.}

\item{specs}{A list of blocks. Each block is a list with \code{prefix} (item
prefix, for example \code{"ANS"}), \code{n_items} (number of items, named
\code{prefix1} to \code{prefixN}), \code{levels} (response options in ascending order)
and \code{start_zero} (logical, scores start at 0 or 1).}
}
\value{
\code{df} with the items of every block scored.
}
\description{
For each block of items, detects the response options with
\code{\link[=detect_expression_Likert]{detect_expression_Likert()}}, builds the score mapping with
\code{\link[=convert_to_expresions]{convert_to_expresions()}} and replaces the text responses by their scores
with \code{\link[=remplace_alternative_response]{remplace_alternative_response()}}.
}
\examples{
df <- data.frame(ANS1 = c("Nunca", "Siempre", "A veces"),
                 ANS2 = c("A veces", "Nunca", "Siempre"))
process_likert_blocks(df, list(
  list(prefix = "ANS", n_items = 2, start_zero = TRUE,
       levels = c("Nunca", "A veces", "Siempre"))
))
}
