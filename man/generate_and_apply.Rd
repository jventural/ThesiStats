% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_and_apply.R
\name{generate_and_apply}
\alias{generate_and_apply}
\title{Add the Sum Score of Each Factor to a Data Frame}
\usage{
generate_and_apply(df, text_lines, new_name = NULL)
}
\arguments{
\item{df}{A data frame with the item columns.}

\item{text_lines}{Character vector, one line per factor.}

\item{new_name}{Deprecated and ignored. Earlier versions also saved the
result in the global environment under this name; assign the returned
data frame instead, for example \code{df2 <- generate_and_apply(df, lines)}.}
}
\value{
\code{df} with one new column per factor (the row sum of its items,
ignoring missing values). The item columns are converted to numeric.
}
\description{
Reads lines of the form \code{"Factor: Item1, Item2, ..."} and adds to \code{df} one
column per factor with the row sum of its items.
}
\examples{
df <- data.frame(A1 = c(1, 2, 3), A2 = c(2, 2, 2), B1 = c(0, 1, 1))
generate_and_apply(df, c("Ansiedad: A1, A2", "Estres: B1"))
}
