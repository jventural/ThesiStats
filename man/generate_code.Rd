% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_code.R
\name{generate_code}
\alias{generate_code}
\title{Generate dplyr Code that Adds Factor Sum Scores}
\usage{
generate_code(text, name = "df_new_renombrado")
}
\arguments{
\item{text}{A single string with one line per factor, separated by \code{"\\n"}.}

\item{name}{Name of the data frame used in the generated code.}
}
\value{
A character string with R code.
}
\description{
Reads text with one line per factor (\code{"Factor: Item1, Item2, ..."}) and
returns, as a string, the dplyr code that adds the row sum of each factor
to a data frame. The code can be printed with \code{\link[=cat]{cat()}} and pasted into a
script.
}
\examples{
codigo <- generate_code("Ansiedad: A1, A2, A3\nEstres: B1, B2", name = "datos")
cat(codigo)
}
