% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_carreras.R
\name{normalize_carreras}
\alias{normalize_carreras}
\title{Normalize Peruvian University Degree Names}
\usage{
normalize_carreras(
  df,
  col_name = "Carrera",
  max_dist = 0.15,
  facultad = FALSE,
  manual_path = NULL,
  force_match = FALSE,
  fallback_dist = 0.4,
  remove_unmatched = FALSE
)
}
\arguments{
\item{df}{A data frame.}

\item{col_name}{Name of the column with the degree names. Defaults to
\code{"Carrera"}.}

\item{max_dist}{Maximum string distance accepted as a match (0 to 1).}

\item{facultad}{Logical. Also add a column with the faculty.}

\item{manual_path}{Optional path to an Excel file with the columns \code{raw}
and \code{normalized}, a manual dictionary applied before the fuzzy matching
(requires the readxl package).}

\item{force_match}{Logical. Retry unmatched values with the more permissive
\code{fallback_dist} (not recommended).}

\item{fallback_dist}{Distance used when \code{force_match = TRUE}.}

\item{remove_unmatched}{Logical. Remove the rows whose degree could not be
normalized.}
}
\value{
\code{df} with the column \verb{<col_name>_norm} (and \verb{<col_name>_facultad}
when \code{facultad = TRUE}) placed after \code{col_name}. Messages report the
values that could not be normalized.
}
\description{
Matches free-text degree names (for example "psicologia", "Ing. de
Sistemas") against the catalog \link{carreras_peruanas} with fuzzy string
matching, and adds a column with the standardized name (and optionally the
faculty).
}
\examples{
df <- data.frame(Carrera = c("psicologia", "Derecho", "zzz"))
normalize_carreras(df)
}
