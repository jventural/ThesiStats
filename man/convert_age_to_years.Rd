% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_age_to_years.R
\name{convert_age_to_years}
\alias{convert_age_to_years}
\title{Convert Age Strings to Numeric Years}
\usage{
convert_age_to_years(
  df,
  col_name = "Edad",
  round_decimals = 2,
  drop_missing = FALSE
)
}
\arguments{
\item{df}{A data frame.}

\item{col_name}{Name of the age column. Defaults to \code{"Edad"}.}

\item{round_decimals}{Number of decimals of the result. Defaults to 2.}

\item{drop_missing}{Logical. If \code{TRUE}, rows whose age could not be
converted are removed; if \code{FALSE} (default) they are kept as \code{NA} and
listed in a message.}
}
\value{
A list with \code{cleaned_df} (the data frame with \code{col_name} converted)
and \code{verify_df} (a tibble pairing each original string with its value).
}
\description{
Parses a character column with ages written in Spanish (for example
"2 años", "un año" or "3,5") into numeric years. Unlike
\code{\link[=convert_age_to_years_months]{convert_age_to_years_months()}}, months are not added.
}
\examples{
df <- data.frame(Edad = c("20 años", "un año", "3,5", "sin dato"))
res <- convert_age_to_years(df)
res$verify_df
}
