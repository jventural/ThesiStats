% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Fiabilidad.R
\name{Fiabilidad}
\alias{Fiabilidad}
\title{Omega Reliability of One Factor}
\usage{
Fiabilidad(vars, data)
}
\arguments{
\item{vars}{Character vector with the names of the items of the factor, for
example one element of the list returned by \code{\link[=extract_items]{extract_items()}}.}

\item{data}{A data frame with the item responses.}
}
\value{
A named numeric value: the omega coefficient of the factor.
}
\description{
Fits a one-factor confirmatory model to the items in \code{vars}, treating them
as ordinal (WLSMV estimator) and returns the composite reliability (omega)
computed by \code{\link[semTools:compRelSEM]{semTools::compRelSEM()}}.
}
\examples{
\donttest{
set.seed(123)
n <- 300
eta <- rnorm(n)
items <- as.data.frame(sapply(1:4, function(j) as.numeric(cut(
  0.7 * eta + rnorm(n, 0, 0.7), c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(items) <- paste0("ANS", 1:4)

Fiabilidad(vars = names(items), data = items)
}
}
\seealso{
\code{\link[=calcula_omega_all]{calcula_omega_all()}} for several factors at once.
}
