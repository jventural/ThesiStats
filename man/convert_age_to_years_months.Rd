% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_age_to_years_months.R
\name{convert_age_to_years_months}
\alias{convert_age_to_years_months}
\title{Convert Age Strings with Years and Months to Numeric Years}
\usage{
convert_age_to_years_months(
  df,
  col_name = "Edad",
  round_decimals = 2,
  drop_missing = FALSE
)
}
\arguments{
\item{df}{A data frame.}

\item{col_name}{Name of the age column. Defaults to \code{"Edad"}.}

\item{round_decimals}{Number of decimals of the result. Defaults to 2.}

\item{drop_missing}{Logical. If \code{TRUE}, rows whose converted age is zero
(entries without any usable number) are removed. Defaults to \code{FALSE}.}
}
\value{
A list with \code{cleaned_df} (the data frame with \code{col_name} converted)
and \code{verify_df} (a tibble pairing each original string with its value).
}
\description{
Parses a character column with ages written in Spanish (for example
"2 años", "6 meses", "1 año 3 meses" or "3,5") into numeric years,
expressing months as fractions of a year.
}
\examples{
df <- data.frame(Edad = c("2 años", "6 meses", "1 año 3 meses", "3,5"))
res <- convert_age_to_years_months(df)
res$verify_df
}
