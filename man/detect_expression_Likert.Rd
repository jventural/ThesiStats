% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_expression_Likert.R
\name{detect_expression_Likert}
\alias{detect_expression_Likert}
\title{Detect Likert Response Options and Assign Scores}
\usage{
detect_expression_Likert(
  df,
  start_zero = TRUE,
  likert_levels = c("Completamente en desacuerdo", "En desacuerdo", "Me es indiferente",
    "De acuerdo", "Completamente de acuerdo")
)
}
\arguments{
\item{df}{A data frame whose columns hold Likert responses as text.}

\item{start_zero}{Logical. Scores start at 0 (\code{TRUE}, default) or at 1.}

\item{likert_levels}{Character vector with the response options in
ascending order.}
}
\value{
A tibble with the columns \code{Alternativas} (the options found, as an
ordered factor) and \code{score}. A warning lists the options of
\code{likert_levels} that do not appear in \code{df}.
}
\description{
Looks in a data frame for the response options listed in \code{likert_levels}
(ignoring case, accents and extra spaces), keeps those that appear, orders
them as in \code{likert_levels} and assigns each one a score starting at 0 or 1.
}
\examples{
respuestas <- data.frame(
  P1 = c("De acuerdo", "En desacuerdo", "Me es indiferente"),
  P2 = c("Completamente de acuerdo", "De acuerdo", "En desacuerdo")
)
suppressWarnings(detect_expression_Likert(respuestas))
}
