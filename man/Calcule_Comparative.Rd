% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Calcule_Comparative.R
\name{Calcule_Comparative}
\alias{Calcule_Comparative}
\title{Compare Two Groups with Welch's t Test and Cohen's d}
\usage{
Calcule_Comparative(data, cols, group_var, Robust = FALSE)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Character vector with the names of the variables to compare.}

\item{group_var}{Name of the grouping variable. It must have exactly two
groups.}

\item{Robust}{Logical. If \code{FALSE} (default), Cohen's d uses the pooled
standard deviation. If \code{TRUE}, the robust effect size of Algina, Keselman
and Penfield (\code{WRS2::akp.effect()}) is reported instead; this requires the
WRS2 package.}
}
\value{
A data frame with one row per variable and the columns
\code{Variables_interes}, \verb{<group 1>(SD1)}, \verb{<group 2>(SD2)}, \code{t}, \code{gl}, \code{p},
\code{d_cohen} and \code{Interpretacion}.
}
\description{
For each variable in \code{cols}, compares the two groups defined by
\code{group_var} with Welch's t test and reports the means and standard
deviations of each group, the t statistic, its degrees of freedom, the
p value and Cohen's d with a verbal interpretation.
}
\details{
The two groups are taken in the order of
\code{levels(factor(data[[group_var]]))}, the same order used by
\code{\link[stats:t.test]{stats::t.test()}}, so the means, standard deviations, the difference and
the sign of d always refer to the same group. The interpretation uses
|d| > .80 "Grande", > .50 "Mediano", > .30 "Pequeno" and "Trivial"
otherwise.
}
\examples{
set.seed(1)
df <- data.frame(
  grupo = rep(c("Mujer", "Varon"), each = 50),
  ansiedad = c(rnorm(50, 20, 4), rnorm(50, 18, 4)),
  depresion = c(rnorm(50, 15, 3), rnorm(50, 15, 3))
)
Calcule_Comparative(df, cols = c("ansiedad", "depresion"), group_var = "grupo")
}
