% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remplace_alternative_response.R
\name{remplace_alternative_response}
\alias{remplace_alternative_response}
\title{Replace Likert Text Responses with Their Scores}
\usage{
remplace_alternative_response(df, columnas_valores_entradas)
}
\arguments{
\item{df}{A data frame.}

\item{columnas_valores_entradas}{A list of blocks. Each block is a list
with two elements: a character vector \code{c(first_column, last_column)}
(for example \code{c("ANS1", "ANS9")}) and the mapping string.}
}
\value{
\code{df} with the responses of the listed columns replaced by their
scores; responses not found in the mapping become \code{NA}.
}
\description{
Converts text responses into numbers in one or more blocks of item columns.
Each block is described by its first and last column and by a string that
maps each response option to its score (\code{"0. Nunca; 1. A veces; ..."}, the
format returned by \code{\link[=convert_to_expresions]{convert_to_expresions()}}).
}
\examples{
df <- data.frame(P1 = c("Nunca", "Siempre"), P2 = c("A veces", "Nunca"))
remplace_alternative_response(
  df, list(list(c("P1", "P2"), "0. Nunca; 1. A veces; 2. Siempre"))
)
}
