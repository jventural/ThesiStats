% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcular_correlaciones.R
\name{calcular_correlaciones}
\alias{calcular_correlaciones}
\title{Correlation Matrix of a Range of Columns}
\usage{
calcular_correlaciones(
  data,
  columna_inicial,
  columna_final,
  method = c("spearman", "pearson"),
  winsorize = FALSE,
  show_pval = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{columna_inicial, columna_final}{Names of the first and last columns of
the range.}

\item{method}{\code{"spearman"} (default) or \code{"pearson"}.}

\item{winsorize}{Logical. With \code{method = "pearson"}, compute 20\% Winsorized
correlations with \code{WRS2::winall()} (requires the WRS2 package).}

\item{show_pval}{Logical. Append significance marks to each correlation:
\code{ns} p > .05, \code{*} p <= .05, \verb{**} p <= .01, \verb{***} p <= .001, \verb{****}
p <= .0001.}
}
\value{
A list with \code{correlation} (data frame with the correlations in the
lower triangle, "-" on the diagonal and \code{NA} above it) and \code{p_values} (the
p values in the same format).
}
\description{
Computes the Spearman or Pearson correlation matrix of the columns that go
from \code{columna_inicial} to \code{columna_final}, optionally with Winsorized
Pearson correlations and significance marks, and returns it in the lower
triangular format used in thesis tables.
}
\examples{
set.seed(1)
x <- rnorm(80)
df <- data.frame(ansiedad = x, depresion = 0.5 * x + rnorm(80),
                 estres = 0.3 * x + rnorm(80))
calcular_correlaciones(df, "ansiedad", "estres", method = "pearson",
                       show_pval = TRUE)
}
